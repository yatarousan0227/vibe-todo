# Conventions

Place project-specific conventions, domain rules, or organizational policies in this directory when they should be referenced by `/sdd.brief`, `/sdd.design`, `/sdd.tasks`, or `/sdd.implement`.
